const ROLES = {
  CUSTOMER: 'customer',
  COMPANY_ADMIN: 'company_admin',
  IFF_STAFF: 'iff_staff',
  IFF_ADMIN: 'iff_admin',
};

const ALL_ROLES = Object.values(ROLES);

module.exports = { ROLES, ALL_ROLES };
